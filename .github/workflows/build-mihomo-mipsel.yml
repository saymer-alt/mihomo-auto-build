name: Build Mihomo for MIPSel

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

# Даём права на создание релизов
permissions:
  contents: write

jobs:
  check-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Check latest Mihomo release
        id: check
        run: |
          LATEST=$(curl -sL "https://api.github.com/repos/MetaCubeX/mihomo/releases/latest" | jq -r '.tag_name')
          echo "latest=$LATEST" >> "$GITHUB_OUTPUT"
          echo "Latest Mihomo: $LATEST"

      - name: Check if already built
        id: check-release
        run: |
          EXISTS=$(curl -sL "https://api.github.com/repos/${{ github.repository }}/releases/tags/${{ steps.check.outputs.latest }}" | jq -r '.tag_name // "not_found"')
          if [ "$EXISTS" = "${{ steps.check.outputs.latest }}" ]; then
            echo "already_built=true" >> "$GITHUB_OUTPUT"
            echo "Version already built, skipping"
          else
            echo "already_built=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Go
        if: steps.check-release.outputs.already_built == 'false'
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache: false

      - name: Install cross-compiler
        if: steps.check-release.outputs.already_built == 'false'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-mipsel-linux-gnu

      - name: Clone and build Mihomo
        if: steps.check-release.outputs.already_built == 'false'
        env:
          VERSION: ${{ steps.check.outputs.latest }}
        run: |
          git clone --depth 1 --branch "$VERSION" https://github.com/MetaCubeX/mihomo.git ./mihomo-src
          cd ./mihomo-src
          ls -la go.mod
          
          CGO_ENABLED=0 GOOS=linux GOARCH=mipsle GOMIPS=softfloat \
          go build -ldflags "-X github.com/MetaCubeX/mihomo/constant.Version=${VERSION} -s -w" \
          -o ../mihomo-linux-mipsel-softfloat

      - name: Create ipk package
        if: steps.check-release.outputs.already_built == 'false'
        env:
          VERSION: ${{ steps.check.outputs.latest }}
        run: |
          VER_CLEAN="${VERSION#v}"
          
          mkdir -p ./ipk/CONTROL
          cat > ./ipk/CONTROL/control << EOF
          Package: mihomo
          Version: ${VER_CLEAN}
          Architecture: mipsel-3.4
          Maintainer: saymer-alt
          Description: Mihomo proxy for Keenetic MT7621
          EOF
          
          mkdir -p ./ipk/opt/sbin
          cp ./mihomo-linux-mipsel-softfloat ./ipk/opt/sbin/mihomo
          chmod +x ./ipk/opt/sbin/mihomo
          
          mkdir -p ./ipk-build
          tar czf ./ipk-build/data.tar.gz -C ./ipk opt
          tar czf ./ipk-build/control.tar.gz -C ./ipk/CONTROL .
          echo "2.0" > ./ipk-build/debian-binary
          
          IPK_NAME="mihomo_${VER_CLEAN}_mipsel-3.4.ipk"
          ar r "./${IPK_NAME}" ./ipk-build/debian
